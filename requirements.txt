torch>=2.9.1
transformers>=5.8.1
datasets>=3.1.0
accelerate>=1.12.0
tokenizers>=0.22.2
# trl>=1.0 breaks LoRA + PEFT's trainable_token_indices on tied lm_head/embed_tokens:
# its new chunked cross-entropy path (sft_trainer.py::_chunked_ce_forward) does
# `lm_head.bias` with no getattr fallback, and PEFT's TrainableTokensWrapper (used to
# train the <think>/</think> embeddings, see scripts/sft/train_thinking_pilot.py) only
# forwards a few known attributes -- not `.bias` -- raising AttributeError. Verified by
# reproducing locally: trl==0.29.1 works, trl==1.8.0 fails with this exact trace.
trl>=0.29.1,<1.0
peft>=0.18.1,<0.19
bitsandbytes>=0.49.1
biopython>=1.85
esm>=3.4.0
pyyaml>=6.0.3
pandas>=2.2.0
scikit-learn>=1.5.0
scipy>=1.13.0
matplotlib>=3.9.0
seaborn>=0.13.0
